{
  "name": "flowgent-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "AI Wizard UX patterns: open-source library + reference implementation",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "test": "pnpm -r --filter \"./packages/*\" test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev:docs": "pnpm --filter docs-site dev",
    "changeset": "changeset",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.5.0"
  }
}
